export enum basketSelectors {
  cartIcon = '[data-menuitem-type="basket"]',
  totalPrice = '[data-test="price-total"]>.price-label__price',
  productPrice = '[data-test="price"]>.product__price-now',
  basketCount = '[data-test="basket-count"]'
}
